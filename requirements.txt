# Runtime
torch>=2.0            # weights_only=True checkpoint loading requires >=1.13
sentencepiece>=0.2.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Test-only (httpx backs fastapi.testclient.TestClient)
pytest>=8.0
httpx>=0.27
